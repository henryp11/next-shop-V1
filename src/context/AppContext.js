import React from "react";
// Creo el contexto global exportando el componente Appcontex

//Creo el contexto con la función desde React de createContext pasando un objeto vacio el cual lo llenaré despues
const Appcontext = React.createContext({});

export default Appcontext;
